const Menu = () => {
  return(
    <nav className="nav">
      <a className="nav-link active">Home</a>
      <a className="nav-link">Flows</a>
      <a className="nav-link">Logout</a>
    </nav>
  )
}

export default Menu;